<div class="comments">
   {{#each this}}
      <section class="comment">
         <div class="text">
            {{comment_text}}
         </div>
         <div class="meta">
            <b>{{user.username}}</b>
            on
            {{format_date created_at}}
         </div>
      </section>
   {{/each}}
</div>